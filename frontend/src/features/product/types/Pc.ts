export type Pc = {
  pcId: number;
  name: string;
  imageUrl: string;
  price: number;
  memory: number;
  storage: number;
  deviceSize: number;
  deviceType: number;
  os: string;
  cpu: string;
  gpu: string;
  purpose: string;
};
